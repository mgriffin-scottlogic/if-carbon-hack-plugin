{
  "name": "examples",
  "version": "1.0.0",
  "description": "Package to contain carbon hack examples and make it easy to run them",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@grnsft/if": "^0.3.1",
    "if-carbon-hack-plugin": "file:.."
  }
}
